# 更新日志

> 本文件同时是应用内「检查更新」弹窗的版本说明来源：
> CI 会用 `tools/gen_release_index.py` 按 tag 抓取对应的 `## <tag>` 段落，
> 写进 Release 的 `index.json`。**每次发版前必须补上对应段落，标题与 tag 完全一致（含 `v`）。**

## v0.1.9

- **安全：内置受保护应用名单（资金交易类硬禁止）**：支付宝、微信、云闪付、数字人民币，以及包名含 `bank` 等特征的国内银行类应用 —— 无论订阅规则（外部引入）或本地规则如何配置，规则引擎对这类应用一律不匹配、不轮询、不点击。名单为引擎层强制，无任何设置可绕过
- 安全：订阅导入审查 —— 引入包含受保护应用规则的订阅时明确提示「N 个受保护应用(支付宝/微信/银行类)的规则已强制拦截」，订阅管理列表的卡片上常驻显示审查结果
- 说明：拦截在引擎层按「当前前台应用」判定，与规则写法完全无关；不影响其他应用的正常跳过功能
- 修正：README 隐私页的网络请求清单中移除了 v0.1.4 已删除的「内置帮助文档」条目，补充仍在使用的快照短链（`f.gkd.li`/`i.gkd.li`）与 HTTP 调试页脚本（npmmirror）
## v0.1.8

- **重要：更换应用包名** `li.songe.gkd` → `io.github.cookiekangd.gkdpure`。此前包名与官方 GKD 相同，部分应用商店会按包名匹配出「官方有新版本」的更新提示；更换后本仓库 Release 独立对应本包，商店不再误报
- ⚠️ 因包名变更，v0.1.7 及更早版本**无法覆盖升级**：需先卸载旧版再安装本版（旧版数据不迁移，规则订阅需重新导入，订阅链接本身不变）
- 代码内部命名空间（`li.gkd.app`）与全部功能逻辑保持不变，规则、快照、无障碍等能力与 v0.1.7 完全一致
## v0.1.7

- 安全（熄屏零操作）：屏幕关闭后丢弃全部无障碍事件并停止一切规则匹配，熄屏瞬间进行中的匹配也会被立即中断 —— 此前上游仅过滤 `CONTENT_CHANGED` 一类事件，`STATE_CHANGED` 等其他事件与「延迟动作」仍可能在熄屏后触发点击
- 修复：触发提示文案模板被清空时回退显示规则名，不再弹出看不见的空提示
- 亮屏恢复：`SCREEN_ON` 广播强制刷新顶部应用与规则，轮询与事件匹配完整恢复，跳过广告的响应能力不受影响

## v0.1.6

- 省电：灭屏后停止规则引擎的强制轮询（此前只要当前应用有可执行规则，即使锁屏也会每 300ms 做一次全节点树匹配，属纯耗电）；亮屏时自动恢复轮询，跳过广告的响应能力不受影响
- 安全终审（自动化链路）：确认订阅规则能力上限仅为屏幕点击 / 长按 / 滑动 / 返回键，规则解析为纯数据、无代码执行，无文本注入 / 联网 / 读取剪贴板通讯录位置等能力；无障碍与快照数据全程本地处理

## v0.1.5

- 图标重绘：改为「大圆角菱形 + 镂空三棒」的单形负空间构图，桌面小尺寸与圆形裁切下均更清晰耐看；启动屏、关于页动画 logo 同步更新
- 修复：`FOREGROUND_SERVICE_MEDIA_PROJECTION` 权限误加 `maxSdkVersion="29"` 导致 Android 14+ 上截屏快照的投屏采集可能静默失败
- 修复：保存截图时 `openOutputStream` 返回空导致的空指针（原先被异常吞掉表现为保存失败）
- 定稿清理（v0.1 最终版）：移除未使用的 appcompat 依赖、空文件与死代码，修正两处强解包
- 安全终审：无障碍事件、控件文本、截图快照全程本地处理；无统计 / 埋点 / 上报组件；无剪贴板 / 通讯录 / 位置等敏感读取

## v0.1.4

- 图标：全新「纯净」风格图标 —— 保留原版菱形 + 三阶条元素，改为圆头胶囊几何与更大留白，启动页、关于页动画 logo 同步替换
- 精简：移除全部指向上游 GKD 文档的站内跳转（首启协议链接、无障碍帮助、订阅帮助、快照帮助、耗电说明等），并整体删除内置网页文档查看器及其 compose-webview 依赖，安装包更小
- 安全：关闭 `allowBackup`，应用私有数据（更新 Cookie、订阅缓存等）不再可被备份提取；首启「使用声明」改为应用内本地隐私说明，不再跳转外部协议页
- 隐私复审：确认读取应用列表仅用于本地规则匹配与界面展示，无任何外传通道；无统计 / 埋点 / 上报 SDK
- 复查：v0.1.3 的四项省电改动逐一复核，确认无逻辑回归（权限状态在应用内变更与返回前台两条路径均会正确刷新）

## v0.1.3

- 省电：去掉「工作模式」页每秒一次的权限轮询（含十几次 binder 调用与一次特权服务跨进程 IPC），改为进入页面 / 回到前台时刷新一次
- 修复：规则匹配可能被重复执行一整轮（`querying` 标志竞态），改为进入同步块即置位，消除重复的规则遍历与节点匹配
- 修复：时间格式化跨线程共用非线程安全的 `SimpleDateFormat`，改为每线程一份，消除并发下的错乱时间串与 `ArrayIndexOutOfBoundsException`
- 优化：应用配置页「按动作时间」排序由 O(分组数 × 日志数) 降为 O(分组数 + 日志数)，避免每次规则命中都触发整轮重排
- 依赖升级（仅运行时库补丁版本，均已通过 CI 编译验证）：compose 1.12.1、nav3 1.1.7、room 3.0.3、sqlite 2.7.1、coil 3.6.2
- CI：`Build-Apk` 不再响应 `main` 分支，发版时不再并排跑两个重复构建（功能分支仍会跑编译冒烟）
- 隐私审计：确认全项目**无任何统计 / 埋点 / 崩溃上报 SDK**，崩溃记录仅存本机；README 新增「隐私与权限」与「省电与性能」章节

## v0.1.2

- 应用名统一为 `gkd_pure`，Release 标题、安装提示、无障碍说明一并同步
- 底栏改为毛玻璃浮岛：滚动内容从浮岛背后透出并被实时模糊（Android 12 以下自动退回半透明底）
- 「关于」页精简为三块：应用简介 / 检查更新 / 上次更新（上一个版本号 + 更新内容）
- 移除「关于」页的 问题反馈、捐赠支持、导出日志、开源代码、使用协议、隐私政策 等外部入口
- 移除首页「了解 GKD」入口，以及只被它使用的文档地址常量
- 发版脚本新增校验：「关于」页的「上次更新」版本号必须与上一个 git tag 一致，防止漏改

## v0.1.1

- 产物更名为 `gkd_pure-v<版本>.apk`，与官方 GKD 的 `gkd-v<版本>.apk` 区分开
- 「检查更新」改为指向本仓库（cookie-kangd/gkd_pure）的 Release，不再检查官方版本
- 下载自动优先走 gh-proxy 镜像加速国内网络，镜像不通时自动回落 GitHub 直链
- 「关于」页精简：去掉已无意义的更新渠道选择（本分支只有一条发布线）
- 「开源代码」与版本信息里的提交链接指向本仓库
- 安装提示：本包与官方 GKD 签名不同，若已装官方版需先卸载

## v0.1

- 基于 [gkd-kit/gkd](https://github.com/gkd-kit/gkd) v1.12.1 的首个 gkd_pure 构建
- 使用本项目独立签名密钥，与官方版本不兼容，无法互相覆盖安装
